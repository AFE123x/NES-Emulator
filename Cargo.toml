[package]
name = "Emulator"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = "2.9.0"

rand = "0.9.0"
sdl2 = {version = "0.37.0"}

[features]
use_sdl2_mac_framework = ["sdl2/use_mac_framework"]
